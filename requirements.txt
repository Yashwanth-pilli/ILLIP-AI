fastapi>=0.104.1
qdrant-client>=1.9.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.23
httpx>=0.28.0
aiofiles>=23.2.1
pypdf>=3.17.1
python-multipart>=0.0.6
aiohttp>=3.9.0
psutil>=5.9.0
ddgs>=9.0.0
python-telegram-bot>=20.0
gtts>=2.5.0

# Connectors — each optional, only needed if that integration is used
discord.py>=2.3.0          # Discord bot (DISCORD_BOT_TOKEN)
slack_sdk>=3.26.0          # Slack bot (SLACK_BOT_TOKEN + SLACK_APP_TOKEN)
aiosmtplib>=3.0.0          # Email SMTP send (EMAIL_ADDRESS + EMAIL_PASSWORD)

trafilatura>=1.8.0             # web page text extraction for research agent

playwright>=1.44.0             # browser automation agent — run: playwright install chromium

# Optional — only needed locally (GPU/mic), skipped gracefully in cloud
faster-whisper>=1.0.0
